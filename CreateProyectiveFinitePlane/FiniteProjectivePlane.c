//
// Created by fish on 25/02/24.
//
#include "FiniteProjectivePlane.h"


int main() {
    setConnections();
    int **cards = setCards();
    /*
    for (int i = 0; i < N; i++) {
        printf("Card #%d : [", i);
        for (int j = 0; j < r; j++) {
            printf("%d, ", cards[i][j]);
        }
        printf("]\n");
    }
    */
    //showConnections(connections);
}
